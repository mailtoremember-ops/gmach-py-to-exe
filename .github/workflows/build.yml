name: Build EXE with Nuitka

on:
  push:
    branches: [ "main", "master" ]
  workflow_dispatch: # מאפשר להפעיל את הקימפול ידנית בלחיצת כפתור

jobs:
  build:
    runs-on: windows-latest # אנו רוצים לקמפל קובץ EXE ל-Windows

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11' # גרסת פייתון יציבה
          architecture: 'x64'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install nuitka

      - name: Build EXE using Nuitka
        run: |
          python -m nuitka `
            --standalone `
            --onefile `
            --windows-disable-console `
            --enable-plugin=tk-inter `
            --include-package-data=ttkbootstrap `
            gmach.py

      - name: Upload EXE as Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Gmach-App-Windows
          path: gmach.exe
